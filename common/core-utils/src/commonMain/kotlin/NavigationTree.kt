object NavigationTree {

    enum class Auth{
        Auth
    }

    enum class User{
        TipsList, FavoriteTips, TipDetail
    }

    enum class Admin{
        TipsList, AddTip, EditTip, TipDetail
    }

}